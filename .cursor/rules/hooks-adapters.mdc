---
description: Hook manifests are per-client adapters; shared scripts only live under hooks/
alwaysApply: true
---

# Hook adapter discipline

Hook **scripts** are shared under `hooks/*.mjs`. Hook **manifests** (`hooks.json`) are per-client adapters, same as MCP and rules.

| Client | Manifest | Declared in |
|---|---|---|
| Claude Code | `hooks/hooks.json` | default discovery |
| Cursor | `clients/cursor/hooks/hooks.json` | `.cursor-plugin/plugin.json` |
| Codex / ChatGPT | `hooks/hooks.json` + `com.openai/hooks/hooks.json` | auto-discovery + Agent Plugins `com.openai` extension |

Do not add client-specific hook events to `hooks/hooks.json`. Codex-only events such as `SubagentStart` belong in `com.openai/hooks/hooks.json` and must use `${PLUGIN_ROOT}`.

`scripts/check.mjs` enforces this. If you add a new client hook surface, add a manifest under that host's adapter path and wire it from the host manifest or extension namespace.
