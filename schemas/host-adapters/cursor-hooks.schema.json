{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ArcadeAI/arcade-plugin/schemas/host-adapters/cursor-hooks.schema.json",
  "title": "Arcade Cursor hook adapter contract",
  "$comment": "Repository-owned contract for the documented Cursor hook subset used here. sessionStart injects session routing; postToolUse can also emit additional_context but is out of scope here. Source checked 2026-09-17: https://cursor.com/docs/hooks",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "hooks"],
  "properties": {
    "version": {
      "const": 1
    },
    "hooks": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sessionStart"],
      "properties": {
        "sessionStart": {
          "type": "array",
          "minItems": 1,
          "maxItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["command", "timeout"],
            "properties": {
              "command": {
                "type": "string",
                "minLength": 1
              },
              "timeout": {
                "type": "integer",
                "const": 5
              }
            }
          }
        }
      }
    }
  }
}
