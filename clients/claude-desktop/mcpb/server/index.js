// Placeholder entry point required by the MCPB manifest schema. Claude Desktop
// launches the server via mcp_config (npx mcp-remote → hosted gateway); this
// file is never executed in normal use. If invoked directly, exit cleanly.
process.exit(0);
